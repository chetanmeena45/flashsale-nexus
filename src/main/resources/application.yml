spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nexus_db
    username: postgres
    password: Chet@324
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

# Add this to keep the console clean and readable
logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG